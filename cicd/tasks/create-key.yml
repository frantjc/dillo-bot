---
platform: linux

image_resource:
    type: registry-image
    source:
        repository: ubuntu

params:
    KEY:

inputs:
    - name: dillo-bot

outputs:
    - name: key

run:
    path: /bin/bash
    args:
        - "dillo-bot/cocd/scripts/create-vars.sh"

