---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine

inputs:
  - name: dillo-bot

outputs:
  - name: dillo-bot-dockerfile

run:
  path: /bin/bash
  args:
    - "dillo-bot/cicd/scripts/copy-dockerfile-to-root.sh"