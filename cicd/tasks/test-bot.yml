---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: openjdk

inputs:
  - name: dillo-bot

caches:
  - path: dillo-bot
  - path: .m2/

run:
  path: /bin/sh
  args: ["dillo-bot/cicd/tasks/test-bot.sh"]