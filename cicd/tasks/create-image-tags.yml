---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine

params:
  ENV:

inputs:
  - name: dillo-bot
  - name: version

outputs:
  - name: labels

run:
  path: /bin/sh
  args:
    - "dillo-bot/cicd/scripts/create-image-tags.sh"